import {useParams} from "./App.jsx";

export default function Checkout() {
    // todo: display cart, and allow removal of cart items
    const { items, cart, setCart } = useParams()
    return (
        <></>
    )
}